server {
	listen {{ demos_port }};
	listen [::]:{{ demos_port }};

	root /var/www/html;

	index index.html index.htm index.nginx-debian.html;

	server_name _;

	location / {
		try_files $uri $uri/ =404;
	}

	location /demos_qwc2 {
		root /home/{{ user }}/{{ prefix }}/demos/qwc2/;
        }
}
